(function(global, $){
	'use strict';
	// $.fn === jQuery.prototype 객체
	// 프로토타입에 메소드를 넣어두고 객체가 불러와서 사용한다.
	// jQuery 프로토타입 객체에 radioClass 메소드가 없다면
	// 조건이 참이 되어 radioClass 메소드가 정의된다.
	function radioClass(){
		
	}
})(this, this.jQuery)